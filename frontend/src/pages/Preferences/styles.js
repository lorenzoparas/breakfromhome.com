import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    websiteInput: {
        fontsize: '36px',
        margintop: '48px',
        borderradius: '8px',
        padding: '18px',
    },
    container: {
        marginTop: '8em',
    }
}));
